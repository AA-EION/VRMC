node_modules/
dist/
.turbo/
*.tsbuildinfo
.DS_Store
.env
.env.local
coverage/

# Unity
unity/Library/
unity/Temp/
unity/Obj/
unity/Build/
unity/Logs/
unity/UserSettings/
*.csproj
*.sln
render-smoke.png

# Bridge packaging output. The scripts under build/ are source and are tracked,
# so this is scoped to the two output directories rather than the whole folder.
apps/desktop-bridge/build/out/
apps/desktop-bridge/build/dist/
